import { SearchMagazinePipe } from './searchMagazine.pipe';

describe('SearchMagazinePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMagazinePipe();
    expect(pipe).toBeTruthy();
  });
});